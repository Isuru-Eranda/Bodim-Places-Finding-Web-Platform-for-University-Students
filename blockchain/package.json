{
  "name": "bodim-blockchain",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "hardhat test",
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network localhost"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.2.2",
    "chai": "^4.3.7",
    "ethers": "^5.7.2",
    "hardhat": "^2.17.0"
  }
}
